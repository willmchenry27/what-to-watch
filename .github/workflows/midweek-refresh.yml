name: Midweek Guide Refresh

on:
  schedule:
    # Every Tuesday at 18:00 UTC — 2:00 PM ET during EDT, 1:00 PM ET during EST
    # (GitHub cron is UTC-only and does not follow DST)
    - cron: '0 18 * * 2'
  workflow_dispatch: # Manual trigger for testing

jobs:
  refresh-guide:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v5

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: '20'

      - name: Install server dependencies
        working-directory: server
        run: npm ci

      - name: Run pipeline (refresh-only)
        env:
          PIPELINE_MODE: refresh-only
          TMDB_API_KEY: ${{ secrets.TMDB_API_KEY }}
          OMDB_API_KEY: ${{ secrets.OMDB_API_KEY }}
          TURSO_DATABASE_URL: ${{ secrets.TURSO_DATABASE_URL }}
          TURSO_AUTH_TOKEN: ${{ secrets.TURSO_AUTH_TOKEN }}
        working-directory: server
        run: node scripts/run-pipeline.js
