EXPO_PUBLIC_API_BASE_URL=http://192.168.1.42:3001

# Recipient token for Save / Seen / Not for me actions.
# On the web app this is harvested from email action links (?r=...).
# On TV, grab the same value from any recent email action link and paste it here.
# Leave blank to disable per-user actions (rows still render).
EXPO_PUBLIC_RECIPIENT_TOKEN=
